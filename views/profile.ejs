<h1><%= user.name %>のプロフィール</h1>
<img src="<%= profile_image %>" alt="icon" width="64" height="64">
<p><%= bio %></p>
<h2>投稿一覧</h2>
<ul>
  <% for(let post of posts){ %>
    <li><%= post.content %> <small><%= post.created_at %></small></li>
  <% } %>
</ul>